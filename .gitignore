# secrets
.env
*.pem
*.key
# cloud service-account / credential keys (Google, AWS, etc.)
*service-account*.json
*credentials*.json
jobradar-*.json
gcp-*.json
*-key.json

# python
__pycache__/
*.pyc
.venv/
venv/

# lambda build artifacts (vendored deps, rebuildable)
build/

# local funnel state / outputs (all regenerable caches)
output/

# os / editor cruft
.DS_Store
.vscode/
.idea/
